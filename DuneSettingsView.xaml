<UserControl x:Class="DunePlayniteAddon.DuneSettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="400">
    <StackPanel Margin="10">
        <TextBlock Text="Server URL:" Margin="0,0,0,5" FontWeight="Bold"/>
        <TextBox Text="{Binding Settings.ServerUrl}" Margin="0,0,0,15" VerticalAlignment="Center"/>

        <CheckBox IsChecked="{Binding Settings.AutoSyncOnClose}" Margin="0,0,0,10">
            <TextBlock Text="Upload saves to server automatically when game closes" TextWrapping="Wrap"/>
        </CheckBox>

        <CheckBox IsChecked="{Binding Settings.SyncOnGameStart}" Margin="0,0,0,10">
            <TextBlock Text="Download latest saves from server before launching a game" TextWrapping="Wrap"/>
        </CheckBox>

        <CheckBox IsChecked="{Binding Settings.OfflineMode}" Margin="0,0,0,10">
            <TextBlock Text="Dual-Boot / Offline Mode (Disables sync natively in Windows if syncing using local Linux dune-server)" TextWrapping="Wrap"/>
        </CheckBox>
    </StackPanel>
</UserControl>
